import React from "react";

const WashingtonNonProfits = ({}) => {
    return (
        <span>
            In Washington, nonprofits must have at least a President and
            Secretary and they
            <strong className="text-danger">cannot</strong>
            be the same person.
        </span>
    );
};

export default WashingtonNonProfits;
